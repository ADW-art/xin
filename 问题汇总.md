# A3 项目问题汇总

> 开发过程中遇到的实际问题及解答，按主题分类。
> 这些问题不适合放入 CLAUDE.md（操作手册）或 项目解释.md（系统教程），所以单独记录。

---

## 目录

1. [Docker 与 WSL](#一docker-与-wsl)
2. [讯飞星火 API](#二讯飞星火-api)
3. [配置思路与方法论](#三配置思路与方法论)
4. [项目架构理解](#四项目架构理解)
5. [报错排查记录](#五报错排查记录)
6. [MCP 协议与配置](#六mcp-协议与配置)
7. [常用命令速查](#七常用命令速查)

---

## 一、Docker 与 WSL

### Q1: 为什么需要装 WSL（Linux 子系统）？

Docker 的容器本质是 Linux 容器，Windows 没法直接跑 Linux 程序。WSL 2 是一个轻量级 Linux 虚拟机，Docker Desktop 用它作为底层引擎。

```
你的 Windows
  └── Docker Desktop
        └── WSL 2（微型 Linux）
              ├── MySQL 容器
              ├── Redis 容器
              ├── MinIO 容器
              └── ChromaDB 容器
```

没有 WSL 2 → Docker Desktop 启动不了。

### Q2: Docker 安装在哪？

Docker 分三部分：

| 部分 | 位置 | 作用 |
|------|------|------|
| Docker CLI | `C:\Program Files\Docker\Docker\` | `docker ps` 命令 |
| Docker Engine | WSL 2 虚拟机里 | 真正跑容器 |
| 数据（镜像/容器/卷） | WSL 2 虚拟磁盘里 | 所有 pull 的镜像 |

### Q3: 为什么数据库用 Docker，但前端后端不 Docker 化？

- **开发阶段**：直接在 Windows 跑，改代码自动热重载，不用每次重新构建镜像
- **数据库**：装一次就配好了，换电脑 `docker compose up -d` 环境完全一样
- **比赛交作品时**再把后端也 Docker 化

### Q4: 哪个模块用到 Docker？

```
前端 (Vue3)      → Windows 原生跑 (npm run dev)
后端 (FastAPI)   → Windows 原生跑 (uvicorn)
数据库们          → Docker 容器跑：
  ├── MySQL    (localhost:3306)
  ├── Redis    (localhost:6379)
  ├── ChromaDB (localhost:8000)
  └── MinIO    (localhost:9000)
```

### Q5: Docker Hub 被墙怎么办？

配国内镜像加速器。Docker Desktop → Settings → Docker Engine：

```json
{
  "registry-mirrors": [
    "https://docker.1ms.run",
    "https://docker.xuanyuan.me"
  ]
}
```

原理：Docker 拉镜像时先去镜像站查缓存，没有才回 Docker Hub。镜像站有国内 CDN，下载速度从 1MB/s → 50MB/s+。

### Q6: `docker compose up -d` 报端口冲突怎么办？

MySQL 默认 3306 端口可能被你电脑已有的 MySQL 占用。

```powershell
# 查谁占用了端口
netstat -ano | findstr 3306

# 两个方案：
# 方案A：停掉旧的
net stop MySQL80

# 方案B：Docker 用别的端口（改 docker-compose.yml）
ports:
  - "3307:3306"   # 宿主机 3307 → 容器 3306
# 然后 .env 里 MYSQL_PORT 也改成 3307
```

### Q7: WSL 装 Ubuntu 时卡 GitHub 超时怎么办？

`raw.githubusercontent.com` 被墙。两个办法：

1. **Microsoft Store** 直接搜 "Ubuntu 22.04 LTS"，点安装（走微软国内 CDN）
2. 用清华/中科大镜像下载 rootfs.tar.gz，再 `wsl --import`

### Q8: WSL 报错 `0x80071772` 怎么办？

虚拟化平台没启用：

```powershell
# 管理员 PowerShell
dism.exe /online /enable-feature /featurename:VirtualMachinePlatform
# 然后重启电脑
```

### Q9: Ubuntu 用户名为什么不能用数字开头？

Linux 用户名规则：必须以小写字母或下划线开头，只能包含小写字母、数字、下划线、短横线。`8534` 不行，`zero` 可以。

---

## 二、讯飞星火 API

### Q10: 讯飞星火 API 为什么用 WebSocket 而不是 HTTP？

大模型生成是一段一段的，WebSocket 让服务端生成一段就推一段，不用等全部生成完。HTTP 是"请求→等全部完成→响应"，不适合流式输出。

### Q11: 讯飞星火的鉴权方式是什么？

HMAC-SHA256 签名，不是简单的 API Key：

```
1. 用 API_SECRET 对 "host + date + request-line" 做签名
2. 把签名 + API_KEY 拼成 authorization 字符串，再 Base64
3. 作为 URL 参数拼到 WebSocket 地址
```

- API_KEY = 身份标识（"我是谁"）
- API_SECRET = 签名密钥（"证明请求确实是我发的"）

### Q12: 讯飞星火显示 "404 No Such Service" 怎么办？

到 console.xfyun.cn → 我的应用 → 服务管理 → 开通"星火认知大模型"。创建的应用默认是空壳，不自动开通任何服务。

### Q13: `test_spark.py` 跑通了但中文乱码？

Windows 终端默认编码是 GBK，不是 UTF-8。不影响实际使用——代码跑在 FastAPI 后端时不会有这个问题，因为 SSE 响应走的是 UTF-8。

---

## 三、配置思路与方法论

### Q14: 配置一个新项目的通用思路是什么？

```
1. 看项目需要什么服务（数据库、缓存、存储）
2. 复制 .env.example 为 .env，填真实值
3. 如果有 docker-compose.yml：
   → 检查 ports 和本地有没有冲突
   → 配镜像加速器
   → docker compose up -d
   → docker compose ps 验证
4. 装语言依赖（pip install / npm install）
5. 启动前后端
```

核心原则：**.env 里数据库配置和 docker-compose.yml 必须一致**。

### Q15: 为什么要用 `.env` 而不是把配置写死在代码里？

1. 密钥（API Key、密码）不能提交到 Git
2. 不同环境配置不同（开发/生产）
3. `.env.example` 提交到 Git 作为模板，`.env` 不提交

### Q16: 配置之间怎么关联的？

```
docker-compose.yml          .env
  mysql:                     MYSQL_HOST=localhost
    ports: 3306:3306   ←→   MYSQL_PORT=3306
    MYSQL_USER: a3_user ←→  MYSQL_USER=a3_user
    MYSQL_PASSWORD: ... ←→  MYSQL_PASSWORD=a3_pass

config.py (Settings) 自动读 .env → 代码里 settings.MYSQL_HOST
```

两边必须一致，对不上就连接失败。

### Q17: Docker 的 `ports: "3306:3306"` 左右分别什么意思？

- 左边（宿主机端口）：你 Windows 上访问的端口
- 右边（容器内端口）：容器里 MySQL 监听的端口

```
localhost:3306 (你的代码) → 端口映射 → 172.18.0.2:3306 (容器里的 MySQL)
```

---

## 四、项目架构理解

### Q18: 这个项目的技术栈为什么这样选？

| 选择 | 原因 |
|------|------|
| FastAPI | 异步性能好 + 自动生成 API 文档 + SSE 原生支持 |
| LangGraph | 状态图可视化（答辩得分点）+ 支持 SSE 流式 |
| 讯飞星火 | 赛题硬约束 |
| MySQL | 结构化的用户画像、学习记录 |
| Redis | 会话管理、热数据缓存 |
| ChromaDB | 轻量 Python 原生向量数据库 |
| BGE 中文模型 | 免费本地跑，中文效果好 |
| Docker | 一键启停所有数据库，环境统一 |

### Q19: 多智能体架构中，Supervisor 的作用是什么？

Supervisor 是调度中枢，不直接生成内容，只负责：

1. 分析用户意图
2. 决定路由到哪个 Agent
3. 收到 Agent 返回后，决定下一步（继续路由 / 结束）

```
用户输入 → Supervisor → 意图分类 → 路由到工作Agent
工作Agent 执行完 → 回到 Supervisor → 再次决策 → 直到 END
```

### Q20: LangGraph 和讯飞星火的关系是什么？

```
LangGraph = 调度层（编排多个 Agent，决定谁先谁后）
讯飞星火 = 执行层（每个 Agent 内部调用它来生成内容）

类比：
  LangGraph = 项目经理（分配任务）
  讯飞星火 = 具体干活的人（写文档、出题、评估）
```

### Q21: 参考项目 `langgraph-AI-interview-agent` 和我们项目的异同？

它是 AI 模拟面试系统，用的也是 Supervisor 多 Agent 模式。

| 对比 | 参考项目 | 我们的项目 |
|------|------|------|
| 大模型 | DeepSeek API | 讯飞星火 WebSocket |
| LLM 接入 | ChatOpenAI 标准接口 | 自封装 SparkClient |
| State 基类 | MessagesState | TypedDict + add_messages |
| Checkpoint | InMemorySaver | RedisSaver |
| 路由方式 | Literal 类型 + Command | str 返回值 |
| 特色功能 | interrupt_before 人工确认 | SSE 流式推送 |

---

## 五、报错排查记录

### Q22: 端口 3306 被占用

```
Error: exposing port TCP 0.0.0.0:3306 → bind: Only one usage...
```

解决：
```powershell
netstat -ano | findstr 3306    # 找到 PID
tasklist | findstr <PID>       # 看进程名
net stop MySQL80               # 停 Windows 服务
# 或者改 docker-compose.yml 端口
```

### Q23: Docker Desktop 启动失败

```
Error response from daemon: Docker Desktop is unable to start
```

可能原因：
1. WSL 没装 → `wsl --install`
2. VirtualMachinePlatform 没启用 → `dism` 命令启用
3. 没重启电脑 → 重启
4. WSL 版本不是 2 → `wsl --set-default-version 2`

### Q24: `wsl --install` 超时

```
无法提取通讯组列表。操作超时
错误代码: Wsl/InstallDistro/WININET_E_TIMEOUT
```

GitHub raw 地址被墙。换 Microsoft Store 安装 Ubuntu，或手动下载 rootfs 导入。

### Q25: Ubuntu 用户名报错

```
Invalid username. A valid username must start with a lowercase letter or underscore
```

用户名必须以小写字母开头。`8534` → 改成 `zero`。

### Q26: `docker: command not found`

在 Claude Code 的 bash 里跑 Docker 命令会报这个。Claude Code 跑在独立的 bash 环境里，不是你的 Windows 终端。

解决：在你自己的 PowerShell/CMD 里跑 Docker 命令。

---

## 六、MCP 协议与配置

### Q27: MCP 是什么？解决什么问题？

**MCP（Model Context Protocol）** 是 Anthropic 制定的一套开放标准协议，让 AI 模型能安全、标准化地调用外部工具和数据源。

**类比**：MCP 就像 USB-C 接口——不管你插的是鼠标、键盘还是硬盘，接口标准统一了，就能即插即用。MCP 让不同的工具（浏览器、数据库、文件系统、API）都能用同一套协议被 Claude 调用。

**MCP 之前**：每个 AI 应用要自己写工具调用逻辑，格式五花八门，换个 AI 就得重写。
**MCP 之后**：工具开发者只需实现一次 MCP 服务，所有支持 MCP 的 AI 都能用。

```
┌──────────────────────────────────────────┐
│              你的 Claude Code              │
│                  (MCP Client)              │
│                                            │
│    ┌──────────┐  ┌──────────┐  ┌───────┐ │
│    │Playwright│  │  MySQL   │  │ GitHub│ │
│    │  MCP     │  │  MCP     │  │  MCP  │ │
│    │ Server   │  │ Server   │  │ Server│ │  ← 更多 MCP Servers...
│    └────┬─────┘  └────┬─────┘  └───┬───┘ │
│         │              │            │      │
│    ┌────▼─────┐  ┌────▼─────┐  ┌───▼───┐ │
│    │ 浏览器    │  │ 数据库    │  │ API   │ │
│    └──────────┘  └──────────┘  └───────┘ │
└──────────────────────────────────────────┘
```

### Q28: MCP 的核心概念有哪些？

MCP 是 Client-Server 架构，有 5 个核心概念：

| 概念 | 角色 | 说明 |
|------|------|------|
| **MCP Host** | 宿主程序 | 运行 AI 模型的程序，如 Claude Code、Claude Desktop |
| **MCP Client** | 协议客户端 | 在 Host 内部，负责与 Server 建立连接、收发消息 |
| **MCP Server** | 工具提供方 | 一个独立进程，暴露一组 Tool/Resource/Prompt |
| **Tool** | 可调用工具 | Server 暴露的函数，Client 可以调用（如 `execute_sql`） |
| **Resource** | 可读取资源 | Server 暴露的数据源（如文件内容、数据库 schema） |

**一次典型调用流程**：

```
用户说"帮我查 learning_profiles 表里有哪些字段"
  → Claude 理解意图
  → Claude 调用 MySQL MCP 的 describe_table 工具
  → MCP Server 执行 DESCRIBE learning_profiles
  → 返回字段列表
  → Claude 解读并告诉你结果
```

### Q29: MCP 有哪几种通信方式？

| 方式 | 原理 | 适用场景 |
|------|------|---------|
| **stdio** | 启动本地子进程，通过标准输入输出传 JSON | 本地工具（数据库、浏览器、文件系统） |
| **SSE** | HTTP Server-Sent Events 长连接 | 远程服务、需要持久连接的场景 |
| **Streamable HTTP** | 新标准，统一了 SSE 和普通 HTTP | 正在推广中，未来主流方式 |

**stdio 是当前最常用的方式**，因为你配置的 Playwright、MySQL 都是本地启动的：

```
Claude Code 启动 MCP Server 进程
  → 通过 stdin 发 JSON-RPC 请求
  → MCP Server 处理后通过 stdout 返回 JSON-RPC 结果
```

具体到 Playwright：
```
Claude Code → npx @playwright/mcp@latest → 启动 Node.js 进程 → 操控 Chromium
```

### Q30: Claude Code 中 MCP 的两种配置方式？

#### 方式一：插件（Plugin）— 从市场安装

MCP Server 打包成 Claude Code 插件，放在市场（Marketplace）里。用户只需在 `settings.json` 的 `enabledPlugins` 中启用。

```
settings.json:
{
  "enabledPlugins": {
    "playwright@claude-plugins-official": true  ← 一行搞定
  }
}
```

插件内部自带了 `.mcp.json`，定义了启动命令。Claude Code 自动读取并启动。

**优点**：一键启用，不用管启动细节。
**适用**：市场中有现成的 MCP Server（Playwright、GitHub、Linear 等）。

#### 方式二：项目级 `.mcp.json` — 手动配置

在项目根目录创建 `.mcp.json`，手写 MCP Server 定义：

```json
{
  "mysql": {
    "command": "npx",
    "args": ["-y", "@benborla29/mcp-server-mysql"],
    "env": {
      "MYSQL_HOST": "localhost",
      "MYSQL_PORT": "3306",
      "MYSQL_USER": "a3_user",
      "MYSQL_PASS": "a3_pass",
      "MYSQL_DB": "a3_learning"
    }
  }
}
```

**优点**：不依赖市场，任何 npm/pip 包都能用。
**适用**：市场中没有的 MCP Server、需要定制环境变量的场景。

#### 两种方式对比

| 维度 | 插件方式 | .mcp.json 方式 |
|------|---------|---------------|
| 配置复杂度 | 一行 JSON | 需要写完整启动配置 |
| 依赖 | 需要市场收录 | 任何 npm/pip 包 |
| 共享性 | 全局可用 | 项目级，跟着项目走 |
| 环境变量 | 插件预设 | 完全自定义 |
| 典型场景 | Playwright、GitHub | 项目专属数据库 |

### Q31: MCP 的配置文件优先级是怎样的？

Claude Code 会合并多个来源的 MCP 配置（低→高优先级）：

```
1. 托管策略（managed-mcp.json）        ← 企业管理员强制配置
2. 用户级 settings.json 的 enabledPlugins ← 你手动启用的插件
3. 项目级 .mcp.json                    ← 项目根目录，跟着代码走
4. 本地 .mcp.json（.claude/ 下）      ← 最高优先级，不提交 Git
```

**对于我们项目**：
- Playwright ← 来自用户级 `enabledPlugins`（方式一）
- MySQL ← 来自项目根 `.mcp.json`（方式二）

两者的配置都会生效，互不冲突。

### Q32: MCP Server 的生命周期是怎样的？

```
1. Claude Code 启动
     ↓
2. 扫描 MCP 配置源
   （settings.json → .mcp.json → managed-mcp.json）
     ↓
3. 合并所有 Server 定义
     ↓
4. 对每个 Server：
   ├── 首次使用？→ 弹窗问用户是否信任
   │   （enableAllProjectMcpServers: true 可跳过）
   ├── 启动子进程（如 npx @playwright/mcp@latest）
   ├── 建立 JSON-RPC 通信通道
   ├── 询问 Server："你能提供哪些工具？"
   ├── Server 返回工具列表（名称+描述+参数 schema）
   └── 将工具注入 Claude 的可用工具列表
     ↓
5. 会话中：
   用户提问 → Claude 判断需要哪个工具 → 调用对应 MCP Tool
     ↓
6. Claude Code 关闭
   → 发送 shutdown 通知给每个 Server
   → 等待进程退出
   → 清理
```

> **关键理解**：MCP Server 是**常驻进程**——启动一次，整个会话期间都在后台运行，不是每次调用才启动。

### Q33: `enableAllProjectMcpServers` 是干什么的？

`.mcp.json` 中定义的 MCP Server，Claude Code 首次发现时会弹窗：

> "此项目包含未批准的 MCP 服务器：mysql。是否批准？"

每次新增 Server 或修改配置都会弹。`enableAllProjectMcpServers: true` 跳过弹窗，自动批准：

```json
// .claude/settings.local.json
{
  "enableAllProjectMcpServers": true
}
```

**安全性**：项目 `.mcp.json` 在项目目录里，只有能改项目的人才能加 MCP Server。所以风险可控——你信任项目代码，就应该信任它的 MCP 配置。

### Q34: MCP 与 Function Calling 有什么区别？

| 维度 | Function Calling | MCP |
|------|-----------------|-----|
| 定义位置 | 嵌入在每次 API 请求里 | 独立 MCP Server 进程 |
| 工具发现 | 代码里硬编码 tools 数组 | Server 动态上报 `tools/list` |
| 跨模型复用 | 每个模型/框架写法不同 | 标准协议，一次编写到处使用 |
| 生命周期 | 跟着 API 请求走 | 独立进程，会话级持久 |
| 安全性 | 依赖代码层的权限控制 | 协议内置权限协商 |
| 生态 | 各厂商各自为政 | 开源社区共建，已 1000+ Server |

**一句话**：Function Calling 是"每次告诉 Claude 有什么工具可用"，MCP 是"Claude 自己去发现有什么工具可用"。

**MCP 底层也是 Function Calling**——MCP Server 上报的工具列表，最终会被 Claude Code 转成 Function Calling 的 tools 数组发给 API。MCP 的价值在于**标准化了工具发现和管理的过程**。

### Q35: JSON-RPC 是什么？MCP 为什么用它？

**JSON-RPC** 是 MCP 的底层通信协议。它是一种极简的远程调用协议：

```json
// 请求（Claude Code → MCP Server）
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "tools/call",
  "params": {
    "name": "execute_sql",
    "arguments": { "query": "SELECT * FROM users LIMIT 5" }
  }
}

// 响应（MCP Server → Claude Code）
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {
    "content": [{ "type": "text", "text": "查询结果: 5 rows returned" }]
  }
}
```

**为什么用 JSON-RPC**：
1. **极简**：没有 HTTP 头、没有状态码——就是纯 JSON
2. **传输无关**：可以跑在 stdio、WebSocket、HTTP 任何通道上
3. **双向**：Server 也可以主动发通知给 Client（如资源变更提醒）
4. **成熟**：20+ 年历史，各语言都有库

### Q36: 我们自己项目配置的两个 MCP Server 分别是什么原理？

#### Playwright MCP

```
启动命令: npx @playwright/mcp@latest
通信方式: stdio（Node.js 子进程）
底层实现: Playwright（Microsoft 的浏览器自动化库）
提供工具: 30+ 个，包括 navigate, screenshot, click, fill, evaluate...

工作原理:
  你的提问: "打开 localhost:5173 截个图"
    → Claude 调用 playwright_navigate(url="http://localhost:5173")
    → MCP Server 操控 Chromium 打开页面
    → Claude 调用 playwright_screenshot()
    → MCP Server 截图，返回图片给 Claude
    → Claude 看到图片，分析 UI 并告诉你结果
```

#### MySQL MCP

```
启动命令: npx -y @benborla29/mcp-server-mysql
通信方式: stdio（Node.js 子进程）
底层实现: mysql2 npm 包连接 MySQL
提供工具: list_tables, describe_table, execute_sql ...

工作原理:
  你的提问: "learning_profiles 表有哪些字段？"
    → Claude 调用 mysql_describe_table(table="learning_profiles")
    → MCP Server 执行 DESCRIBE learning_profiles
    → 返回字段列表
    → Claude 解读给你听
```

### Q37: MCP 生态现状如何？

| 指标 | 数据 |
|------|------|
| 官方/社区 MCP Server | 1000+ |
| 官方市场内置 MCP | Playwright, GitHub, GitLab, Linear, Asana, Firebase, Discord, Telegram 等 |
| 主流数据库 MCP | MySQL, PostgreSQL, SQLite, MongoDB, Redis 等 |
| 开发工具 MCP | Docker, Kubernetes, Terraform, Cloudflare 等 |
| 搜索引擎 MCP | Brave Search, Tavily, Exa 等 |

**查找 MCP Server 的资源**：
- [Smithery.ai](https://smithery.ai) — MCP Server 搜索引擎
- [LobeHub MCP](https://lobehub.com/zh/mcp) — 中文 MCP Server 索引
- [Awesome MCP Servers](https://github.com/punkpeye/awesome-mcp-servers) — GitHub 精选列表
- npm 搜 `mcp-server` — 社区发布的大量 MCP Server

### Q38: MCP 安全性怎么保证？

1. **用户审批机制**：首次使用 MCP Server 会弹窗确认，防止恶意 Server 静默安装
2. **进程隔离**：MCP Server 是独立进程，崩溃不影响 Claude Code
3. **权限最小化**：
   - MySQL MCP 默认只允许 SELECT（读），写操作需要显式配置 `ALLOW_INSERT_OPERATION=true`
   - Playwright MCP 限制在测试浏览器实例中，不影响你的日常浏览器
4. **环境变量隔离**：MCP Server 只能访问你显式传给它的环境变量
5. **`enableAllProjectMcpServers` 作用域限制**：只跳过"批准弹窗"，不跳过安全沙箱

**我们项目的安全实践**：
- `.mcp.json` 中的 MySQL 密码 `a3_pass` 是开发环境默认密码（来自 docker-compose.yml），生产环境会换
- `enableAllProjectMcpServers: true` 放在 `settings.local.json`（不提交 Git），不是 `settings.json`（提交 Git）

---

## 七、常用命令速查

> 本项目日常开发用到的命令。不是教程，是速查表。忘了随时回来看。

### 7.1 Git Bash 路径对照

```
Windows 路径              Git Bash 路径
E:\code\claude-1     →   /e/code/claude-1
E:\                  →   /e/
C:\Users\18534       →   /c/Users/18534
```

**规则**：盘符 `E:` → `/e/`，反斜杠 `\` → 正斜杠 `/`。

### 7.2 Git Bash 基础操作

| 命令 | 作用 |
|------|------|
| `pwd` | 当前在哪个目录 |
| `ls` | 列出当前目录文件 |
| `ls -la` | 列出所有文件（含隐藏、含详情） |
| `cd /e/code/claude-1` | 切到项目目录 |
| `cd ..` | 返回上一级 |
| `clear` | 清屏 |
| `cat 文件名` | 查看文件内容 |

**VS Code 里打开 Git Bash**：`Ctrl+`` → 点终端右上角 `▾` → 选 Git Bash。

### 7.3 项目基础设施（Docker）

```bash
# 进项目目录（每次先做这一步）
cd /e/code/claude-1/a3-learning-system

# 启动所有服务（后台运行）
docker compose up -d

# 查看运行状态
docker compose ps

# 停止所有服务（数据保留）
docker compose stop

# 重新启动
docker compose start

# 停止并删除容器（数据卷保留）
docker compose down

# 停止并删除容器 + 清空所有数据 ⚠️
docker compose down -v

# 只看某个服务的日志
docker compose logs mysql
docker compose logs redis

# 重启单个服务
docker compose restart mysql
```

**日常流程**：
```
开机 → 开 Docker Desktop → cd /e/code/claude-1/a3-learning-system → docker compose up -d → 写代码
关机前 → docker compose stop（或直接关机也行）
```

### 7.4 Docker 状态验证

```bash
# Docker 引擎是否在运行
docker ps
# 正常：显示表头（NAME STATUS PORTS），下面可能空
# 异常：Error response from daemon: ...  → Docker Desktop 没启动

# 四个服务应该都是 Up
docker compose ps
# 期望：
# a3-mysql      Up   0.0.0.0:3307->3306/tcp
# a3-redis      Up   0.0.0.0:6379->6379/tcp
# a3-minio      Up   0.0.0.0:9000->9000/tcp, 0.0.0.0:9001->9001/tcp
# a3-chromadb   Up   0.0.0.0:8000->8000/tcp
```

**端口映射怎么读**：`0.0.0.0:3307->3306/tcp` = "你电脑的 3307 端口 → 转发到容器里的 3306 端口"。代码连 `localhost:3307`。

### 7.5 Python 环境

```bash
# 进后端目录
cd /e/code/claude-1/a3-learning-system/backend

# 创建虚拟环境（第一次）
python -m venv venv

# 激活虚拟环境（每次写代码前）
source venv/Scripts/activate     # Git Bash 下

# 安装依赖（第一次、或 requirements.txt 更新后）
pip install -r requirements.txt

# 启动后端
uvicorn app.main:app --reload --port 8000

# 验证后端
# 浏览器打开 http://localhost:8000/docs
# 或 curl http://localhost:8000/api/health
```

### 7.6 前端环境

```bash
# 进前端目录
cd /e/code/claude-1/a3-learning-system/frontend

# 安装依赖（第一次）
npm install

# 启动开发服务器
npm run dev

# 浏览器打开 http://localhost:5173
```

### 7.7 测试与验证

```bash
# 测试讯飞星火 API 连通性
cd /e/code/claude-1/a3-learning-system
python test_spark.py

# 看后端 API 文档（后端启动后）
# 浏览器打开 http://localhost:8000/docs
```

### 7.8 每次开发的启动清单

| 序号 | 命令 / 操作 | 验证 |
|------|-----------|------|
| 1 | 开 Docker Desktop | 鲸鱼图标稳定 |
| 2 | `docker ps` | 不报错 |
| 3 | `cd /e/code/claude-1/a3-learning-system` | — |
| 4 | `docker compose up -d` | 4 个 Done |
| 5 | `docker compose ps` | 4 个 Up |
| 6 | `cd backend && source venv/Scripts/activate` | 前缀出现 `(venv)` |
| 7 | `uvicorn app.main:app --reload --port 8000` | `Uvicorn running on...` |
| 8 | `cd ../frontend && npm run dev` | `VITE ready in...` |

---

> **使用说明**：
> - 遇到问题先查本文件对应分类
> - 本文件随开发进度持续更新
> - 技术教程放在 `项目解释.md`，操作手册放在 `CLAUDE.md`
