# A3 项目最终问题清单

> 基于全量代码审计（4个Agent并行覆盖API/Agent/前端/Services/Models/Core）
> 对标第十五届中国软件杯A3赛道省一标准

---

## 致命级（必须修复才能提交）

| # | 文件:行 | 问题 | 影响 |
|:--:|------|------|------|
| 1 | `profile_agent.py:147,164` | `_compute_dimension_scores` 硬编码 `get_tracker(0)` — 所有用户的画像维度分从user_id=0的BKT计算 | **所有用户的knowledge/logic/practice分数完全错误** |
| 2 | `path_agent.py:643` | `from app.services.path_agent import _load_multidiscipline_kg` 路径错误，应为 `app.agents.path_agent` | **动态路径重规划中KG加载永远失败，被except静默吞掉** |
| 3 | `recommend.py:17` | `payload.get("user_id", 0)` — JWT令牌中不存在此键，应为 `payload["sub"]` | **推荐功能完全失效，始终返回空数据** |
| 4 | `dashboard.vue` 模板 | 4个重复的 `rec-section` 块导致同一UI渲染4次 | **Dashboard显示垃圾UI** |
| 5 | `video_service.py:14` | `from app.services.tts_service import synthesize_sync` — 函数不存在，应为 `synthesize_speech` | **视频生成功能彻底崩溃** |

## 高危级（严重扣分风险）

| # | 文件:行 | 问题 | 影响 |
|:--:|------|------|------|
| 6 | `shared_utils.py:18-130` | `_normalize_concept_name` 与 `bkt_service.py:1221` 的 `normalize_concept_name` 完全重复，使用不同策略 | **同一概念在不同模块产生不同标准化名称，数据不一致** |
| 7 | `review_schedule.py:27` | 模型默认值 `memory_strength=0.5` 与代码默认值 `4.0` 冲突 | **数据库直接写入时使用错误的记忆强度** |
| 8 | `profile_agent.py:131` | `float(v)` 对非数字值抛出 ValueError | **knowledge_base含字符串值时维度计算崩溃** |
| 9 | `profile_agent.py:182` | `tracker` 在try/except内定义，外部引用可能 `NameError` | **BKT数据获取失败后practice_score静默回退** |
| 10 | `CustomGraphView.vue:229,274` | 4处 `console.log` + 6处 `alert()` 在生成环境 | **评委看到alert弹窗直接扣分** |
| 11 | `rag_service.py:577` | `_embed([query])[0]` — 嵌入失败时 IndexError | **习题检索崩溃** |
| 12 | `rate_limit.py:166` | 每次请求创建新Redis连接，无连接池 | **高并发时Redis连接耗尽** |

## 中危级（影响质量评分）

| # | 文件:行 | 问题 |
|:--:|------|------|
| 13 | `resource_agent.py:23-30` | TYPE_LABELS缺少"comparison"类型映射 |
| 14 | `question_agent.py:469` | `parse_grading_result` 回退假定正确全在最前 → BKT可能用错误题号更新 |
| 15 | `path_agent.py:182` | C语言错误映射到 `kg_cpp.json`（应独立） |
| 16 | `path_agent.py:200` | 数学错误映射到 `kg_algorithm.json` |
| 17 | `supervisor.py:322` | `_proactive_suggest` 检查不存在的 `profile_data` 键 |
| 18 | `collaboration.py:298` | `_quality_review_node` 不检查实际生成内容，只看BKT统计 |
| 19 | `rag_service.py / content_store.py` | BM25代码完全重复 |
| 20 | `answer_record.py:18` | `concept` 列缺少索引（最常查询的列） |
| 21 | `node_resource.py:17` | 只有ForeignKey无ORM relationship，级联删除不生效 |
| 22 | `assessment.py:40-42` | `submit_answer` 中 `db.commit()` 未包裹try/except |
| 23 | `faiss_client.py:204` | `get_faiss()` 非线程安全 |
| 24 | `spark_client.py:54-59` | `_is_retryable()` 与 `API_RETRY` 装饰器不一致 |
| 25 | `ChatInput.vue:160` | `console.error` 遗留在生产代码中 |
| 26 | `ResourceView.vue:113` | 防抖定时器在组件卸载时未清理 |

## 低危级（答辩前可修）

| # | 文件:行 | 问题 |
|:--:|------|------|
| 27 | `profile_agent.py:60-84` | 重复的 `_compute_dimension_scores` 定义（死代码） |
| 28 | `evaluation_agent.py:135` | `_SPEED_CACHE_TTL=30` 注释说300s/5分钟 |
| 29 | `evaluation_agent.py:370-371` | 访问BKT私有属性 `_recent_results` |
| 30 | `evaluation_agent.py:244` | `.all()` 无limit，可能OOM |
| 31 | `path_agent.py:416-425` | `_reorder_by_profile` 仅相邻交换，非全局排序 |
| 32 | `supervisor.py:639` | `additional_kwargs` 中的agent名称可能为空 |
| 33 | `collaboration.py:294` | QA审阅评分：笔记越多分越低 |
| 34 | `learning_path.py:210` | `GET /api/path/debug/simulate` 死端点 |
| 35 | `admin.py:23` | `os.makedirs(KB_DIR)` 在导入时执行 |
| 36 | `chroma_client.py:47-58` | `get_collection()` 三重try/except脆弱 |

---

## 测试层问题

| # | 文件:行 | 问题 |
|:--:|------|------|
| 37 | `conftest.py` | **无测试数据库隔离** — 测试直接写入生产数据库 |
| 38 | `conftest.py:86-90` | `freeze_time_with_progression` 缺少 `side_effect=datetime` |

---

## 汇总

| 级别 | 数量 | 最严重的问题 |
|------|:--:|------|
| 致命 | **5** | 画像分全错 / 路径重规划失效 / 推荐功能崩溃 / Dashboard UI垃圾 / 视频生成崩溃 |
| 高危 | **7** | 概念名重复 / 记忆强度冲突 / 维度计算崩溃 / alert弹窗 / 习题检索崩溃 / Redis泄漏 |
| 中危 | **14** | BM25重复 / 索引缺失 / 级联失效 / 线程不安全 / 死代码 |
| 低危 | **10** | 死代码 / 注释不一致 / 性能隐患 |
| 测试 | **2** | 无DB隔离 / 夹具bug |

**致命5项 + 高危7项 = 必须修复12项。修复后项目省一稳。**
